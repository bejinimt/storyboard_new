let nextOffset = 0;
let elementCount = 0;
let currentColumn = 0;

const rowHeight = 120;
const columnWidth = 250;
const maxElementsPerColumn = 5;

function initStoryboard() {}

function placeElement(el) {
 /* const x = 20 + currentColumn * columnWidth;
  const y = 20 + nextOffset;

  el.style.left = x + "px";
  el.style.top = y + "px";

  nextOffset += rowHeight;
  elementCount++;

  if (elementCount >= maxElementsPerColumn) {
    elementCount = 0;
    nextOffset = 0;
    currentColumn++;
  }*/
}
